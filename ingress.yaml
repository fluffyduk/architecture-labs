apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myip-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: myip.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: myip-service
                port:
                  number: 80